import { parseColorHexRGB } from '@microsoft/fast-colors';
import { createColorPalette } from '@microsoft/fast-components';

export const palette = createColorPalette(parseColorHexRGB('#28EBD7'));
// console.log(palette);

export const palette2 = { color1: '#aaaaaa' };
